# Copy to .env for local tooling (not loaded by the browser directly).
# In the app: Settings → Cloud API, or js/core/supabase-secrets.js (see supabase-secrets.example.js).
SUPABASE_URL=https://YOUR_PROJECT.supabase.co
SUPABASE_ANON_KEY=YOUR_SUPABASE_ANON_KEY
