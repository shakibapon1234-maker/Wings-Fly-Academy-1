// Optional local Supabase credentials (empty by default — no 404 on load).
// Prefer: Settings → Security & Access → Cloud API (encrypted on device).
// For file-based keys, copy from supabase-secrets.example.js and fill url + anonKey.
window.WFA_SUPABASE_SECRETS = window.WFA_SUPABASE_SECRETS || {};
